'use client'

import * as React from 'react'

// ThemeProvider stub: next-themes removed, keep a noop provider to avoid import errors
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
